import React from 'react'
import styled from 'styled-components';

const PolicyText = ({text}) => {
  return (
    <PolicyTextStyle>{text}</PolicyTextStyle>
  )
}

export default PolicyText

const PolicyTextStyle = styled.section`


`;