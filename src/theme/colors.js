import { css } from "styled-components";

export default css`
  :root {
    --white: white;
    --black: black;
    --blue: #141363;
    --grey: rgba(255,255,255,0.5);
    --dark-grey: rgba(255,255,255,0.6);
  }
`;
